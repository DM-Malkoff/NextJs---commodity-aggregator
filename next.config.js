/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    //domains: ['cms.moscow-shiny.ru'],
    domains: ['cms.kolesa-shiny-diski.ru'],
  }
}

module.exports = nextConfig
